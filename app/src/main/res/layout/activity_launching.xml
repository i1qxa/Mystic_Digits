<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.first.LaunchingActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/loading_bckgr"
        android:scaleType="centerCrop"
        />

    <TextView
        android:id="@+id/tvLoadingMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="25dp"
        android:text="@string/loading"
        android:textColor="@color/white"
        android:fontFamily="@font/aclonica_regular"
        android:textSize="40sp"
        android:textStyle="bold"
        />

    <TextView
        android:id="@+id/tvLoadingD"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/tvLoadingMain"
        app:layout_constraintTop_toTopOf="@id/tvLoadingMain"
        app:layout_constraintBottom_toBottomOf="@id/tvLoadingMain"
        android:textColor="@color/white"
        android:fontFamily="@font/aclonica_regular"
        android:textSize="40sp"
        android:textStyle="bold"
        android:text="..."
        android:gravity="bottom"
        />

</androidx.constraintlayout.widget.ConstraintLayout>